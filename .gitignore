# Pre-reins implementation backup (local only)
bak/

# Test fixture — external backend copy (local only)
test/

# Binary
huma
testdata/server/server

# Local state
.huma/
.tsma/
.whyso/

# Runtime quest state (reins session + export results)
session.json
*-results.jsonl

# Local config
.clari/
.claude/
CLAUDE.md

# Plans
plans/

# Scratch notes (local only)
files/

# Environment
.env
